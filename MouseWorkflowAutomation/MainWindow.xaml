<Window x:Class="MouseWorkflowAutomation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MouseWorkflowAutomation"
        mc:Ignorable="d"
        Title="Mouse Workflow Automation" Height="600" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New" Click="New_Click"/>
                <MenuItem Header="_Open" Click="Open_Click"/>
                <MenuItem Header="_Save" Click="Save_Click"/>
                <Separator/>
                <MenuItem Header="_Exit" Click="Exit_Click"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Gainsboro" BorderThickness="0,0,1,0">
                <StackPanel>
                    <TextBlock Text="Toolbox" FontWeight="Bold" Margin="5"/>
                    <Button Content="Click" Margin="5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Move" Margin="5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Drag &amp; Drop" Margin="5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Image Click" Margin="5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Right Click" Margin="5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Delay" Margin="5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" BorderBrush="Gainsboro" BorderThickness="0,0,1,0">
                <Canvas x:Name="WorkflowCanvas" Background="WhiteSmoke" AllowDrop="True" 
                        DragEnter="WorkflowCanvas_DragEnter" DragOver="WorkflowCanvas_DragOver" Drop="WorkflowCanvas_Drop"/>
            </Border>

            <Border Grid.Column="2">
                <ContentControl x:Name="PropertiesPanel"/>
            </Border>
        </Grid>
    </Grid>
</Window>